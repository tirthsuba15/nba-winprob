# data is large + reproducible from fetch_data.py -- don't commit it
# (anchored to repo root so src/data/ source code is NOT ignored)
/data/
*.csv
*.parquet
*.duckdb

# but DO keep the committed example outputs for the README
!outputs/*.csv

# secrets -- never commit API keys
.env
*.key

# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# os
.DS_Store

# local plugin artifact
ruvector.db
